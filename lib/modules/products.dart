class Products {
  String id, name, description, price, color, size;

  Products(
      {this.id,
      this.name,
      this.description,
      this.size,
      this.color,
      this.price});
      
}
